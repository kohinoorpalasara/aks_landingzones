apiVersion: v1
kind: ResourceQuota
metadata:
  name: preprod-quota
  namespace: preprod
spec:
  hard:
    requests.cpu: "12"
    requests.memory: 24Gi
    limits.cpu: "24"
    limits.memory: 48Gi
    pods: "120"
